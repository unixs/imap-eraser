require "spec_helper"

describe ImapEraser do
  it "has a version number" do
    expect(ImapEraser::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
